/**Declear an array and assign values at same time */
const fruitBasket = ['orange', 'banana', 'mango', 'pawpaw', 'guava', 'tengarine'];
const firstFruitInTheArray = fruitBasket[0]; //get the first fruit and save it
const fruitBasketSize = fruitBasket.length; //get the size or length of the basket

/**Below Print each single fruit out of the basket */
fruitBasket.forEach(fruit => {
    console.log(fruit);
})

let namesArray = []; //decleae an empty array
namesArray[0] = "james"; //we are adding james to index 0 of the namesArray
namesArray[1] = "Juliet"; //we are putting juliet in the array
namesArray[2] = "Joshua"; //we are putting joshua to index 2 of the namesArray
